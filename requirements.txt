# RABIT-PROPFIRM DRL — Python Dependencies
# For RunPod deployment (PyTorch template, CUDA 12.x)
# Install: pip install -r requirements.txt

# Core ML
torch>=2.0.0
numpy>=2.0.0

# Data Processing
polars>=1.0.0

# Monitoring
wandb>=0.17.0

# Testing
pytest>=8.0.0

# Utilities (stdlib, no install needed): json, pathlib, sys, time, math, collections, dataclasses, logging, argparse
